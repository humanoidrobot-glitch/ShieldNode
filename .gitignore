# Rust
node/target/
client/src-tauri/target/
**/*.rs.bk

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# Node / Frontend
node_modules/
dist/
client/src-tauri/gen/

# Circuits (cloned deps + build artifacts)
circuits/circomlib/
circuits/circom-ecdsa/
circuits/keccak256-circom/
circuits/build/
# Trusted setup ceremony artifacts (dev and production) — NEVER commit
# These include toxic waste (ptau contributions) and proving keys.
# The Groth16Verifier.sol in contracts/src/ is auto-generated and safe to track.
circuits/trusted_setup/

# ZK-VM (RISC Zero build artifacts)
zkvm/target/

# Environment & secrets
.env
.env.*
*.key
*.pem

# Node operator config (may contain private keys)
node/config.toml
node/my-config.toml
node/data/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code
CLAUDE.md
.claude/
